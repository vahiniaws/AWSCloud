server {
    listen 443 ssl;
    ssl_certificate {{ ssl_certificate_path }};
    ssl_certificate_key {{ ssl_key_path }};
    location / {
        root /var/www/html;
        index index.html;
    }
}
